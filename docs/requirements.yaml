name: docs_Multiscale Polymer Toolkit
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  
  - pydata-sphinx-theme
  - sphinx-autoapi
  - sphinx-design
  - sphinx-copybutton
  - nbsphinx
  - ipython_pygments_lexers




    # Pip-only installs
  - pip:
    - -e ../
