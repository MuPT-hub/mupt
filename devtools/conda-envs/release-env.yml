name: mupt-env
channels:
  - conda-forge
  - defaults
dependencies:
  # Basic Python dependencies
  - python>=3.12
  - pip
  - jupyterlab

    # Unit testing
  - pytest

  # Scientific computing
  - numpy>=2.0
  - scipy
  - networkx

  # Visualization
  - rich
  - matplotlib
  - ipympl

  # Molecular libraries
  - rdkit

  # - pip:
      # - scipy>=1.16.0 # plan to use eventually for new features
