name: test
channels:

  - defaults
dependencies:
  # Base depends
  - python>=3.12,<3.14  # <3.14 because of https://github.com/openforcefield/openff-units/pull/178
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - nbval

    # Pip-only installs
  #- pip:
  #  - codecov

